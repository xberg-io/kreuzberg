apiVersion: v1
kind: Service
metadata:
  name: {{ include "xberg.fullname" . }}
  labels: {{- include "xberg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "xberg.selectorLabels" . | nindent 4 }}
