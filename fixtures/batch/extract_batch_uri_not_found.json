{
	"id": "extract_batch_uri_not_found",
	"category": "batch",
	"description": "extract_batch with missing URI input",
	"tags": ["batch", "uri", "error"],
	"call": "extract_batch",
	"input": {
		"inputs": [
			{
				"kind": "uri",
				"uri": "/nonexistent/a.pdf"
			}
		]
	},
	"assertions": [
		{
			"type": "not_error"
		}
	],
	"skip": {
		"languages": ["wasm"],
		"reason": "WASM cannot access filesystem; use bytes input with file content instead"
	}
}
