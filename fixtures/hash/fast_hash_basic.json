{
  "id": "fast_hash_basic",
  "category": "hash",
  "description": "Hash PDF file using fast hash",
  "tags": ["hashing", "fast_hash", "pdf"],
  "call": "fast_hash",
  "input": {
    "data": "pdf/fake_memo.pdf"
  },
  "assertions": [
    {
      "type": "not_error"
    }
  ],
  "skip": {
    "languages": [],
    "reason": "Tests hash call surface that isn't re-exported at the kreuzberg crate root \u2014 skipped everywhere until Rust public API or codegen module path is updated."
  }
}
