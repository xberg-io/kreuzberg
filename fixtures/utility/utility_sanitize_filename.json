{
  "id": "utility_sanitize_filename",
  "category": "utility",
  "description": "Sanitize filename removes invalid characters",
  "tags": ["utility", "filename", "sanitization"],
  "call": "sanitize_filename",
  "input": {
    "args": ["file<>:name*.txt"]
  },
  "skip": {
    "languages": [],
    "reason": "Tests utility call surface that isn't re-exported at the kreuzberg crate root \u2014 skipped everywhere until Rust public API or codegen module path is updated."
  },
  "assertions": [
    {
      "type": "not_error"
    },
    {
      "type": "min_length",
      "field": "result",
      "value": 1
    }
  ]
}
