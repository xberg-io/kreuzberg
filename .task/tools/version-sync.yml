version: "3"

includes:
  _cfg:
    taskfile: ../config/vars.yml
    internal: true

tasks:
  sync:
    desc: Sync version from Cargo.toml to all package manifests
    cmds:
      - uv run --no-project python kreuzberg/scripts/sync_versions.py
