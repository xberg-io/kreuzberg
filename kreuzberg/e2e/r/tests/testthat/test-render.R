# Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
# To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang r
#
# Tests for render fixtures.
# nolint start

test_that("render_custom_dpi", {
  document_path <- resolve_document("pdf/tiny.pdf")
  if (!file.exists(document_path)) {
    testthat::skip(paste0("missing document at ", document_path))
  }
  png_data <- render_pdf_page(document_path, 0, dpi = 72)
  assert_is_png(png_data)
  assert_min_byte_length(png_data, 50)
})

test_that("render_iterator", {
  document_path <- resolve_document("pdf/tiny.pdf")
  if (!file.exists(document_path)) {
    testthat::skip(paste0("missing document at ", document_path))
  }
  pages <- list()
  render_pdf_pages_iter(document_path, dpi = 150, callback = function(page_index, png_data) {
    assert_is_png(png_data)
    pages[[length(pages) + 1L]] <<- png_data
  })
  testthat::expect_gte(length(pages), 1)
})

test_that("render_single_page", {
  document_path <- resolve_document("pdf/tiny.pdf")
  if (!file.exists(document_path)) {
    testthat::skip(paste0("missing document at ", document_path))
  }
  png_data <- render_pdf_page(document_path, 0, dpi = 150)
  assert_is_png(png_data)
  assert_min_byte_length(png_data, 100)
})
# nolint end
