// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang rust

// Tests for token_reduction fixtures.
#![allow(clippy::too_many_lines)]
use e2e_rust::{assertions, resolve_document};
use kreuzberg::core::config::ExtractionConfig;

#[test]
fn test_token_reduction_aggressive() {
    // Tests aggressive token reduction mode significantly reduces content

    let document_path = resolve_document("pdf/fake_memo.pdf");
    if !document_path.exists() {
        println!(
            "Skipping token_reduction_aggressive: missing document at {}",
            document_path.display()
        );
        return;
    }
    let config: ExtractionConfig = serde_json::from_str(
        r#"{
  "token_reduction": {
    "mode": "aggressive"
  }
}"#,
    )
    .expect("Fixture config should deserialize");

    let result = match kreuzberg::extract_file_sync(&document_path, None, &config) {
        Err(err) => panic!("Extraction failed for token_reduction_aggressive: {err:?}"),
        Ok(result) => result,
    };

    assertions::assert_expected_mime(&result, &["application/pdf"]);
    assertions::assert_min_content_length(&result, 5);
    assertions::assert_max_content_length(&result, 150);
    assertions::assert_content_not_empty(&result);
}

#[test]
fn test_token_reduction_basic() {
    // Tests basic token reduction on PDF document

    let document_path = resolve_document("pdf/fake_memo.pdf");
    if !document_path.exists() {
        println!(
            "Skipping token_reduction_basic: missing document at {}",
            document_path.display()
        );
        return;
    }
    let config: ExtractionConfig = serde_json::from_str(
        r#"{
  "token_reduction": {
    "mode": "moderate"
  }
}"#,
    )
    .expect("Fixture config should deserialize");

    let result = match kreuzberg::extract_file_sync(&document_path, None, &config) {
        Err(err) => panic!("Extraction failed for token_reduction_basic: {err:?}"),
        Ok(result) => result,
    };

    assertions::assert_expected_mime(&result, &["application/pdf"]);
    assertions::assert_min_content_length(&result, 5);
    assertions::assert_max_content_length(&result, 200);
    assertions::assert_content_not_empty(&result);
}

#[test]
fn test_token_reduction_light() {
    // Tests light token reduction mode preserves most content

    let document_path = resolve_document("pdf/fake_memo.pdf");
    if !document_path.exists() {
        println!(
            "Skipping token_reduction_light: missing document at {}",
            document_path.display()
        );
        return;
    }
    let config: ExtractionConfig = serde_json::from_str(
        r#"{
  "token_reduction": {
    "mode": "light"
  }
}"#,
    )
    .expect("Fixture config should deserialize");

    let result = match kreuzberg::extract_file_sync(&document_path, None, &config) {
        Err(err) => panic!("Extraction failed for token_reduction_light: {err:?}"),
        Ok(result) => result,
    };

    assertions::assert_expected_mime(&result, &["application/pdf"]);
    assertions::assert_min_content_length(&result, 10);
    assertions::assert_content_not_empty(&result);
}

#[test]
fn test_token_reduction_with_chunking() {
    // Tests token reduction combined with chunking

    let document_path = resolve_document("pdf/fake_memo.pdf");
    if !document_path.exists() {
        println!(
            "Skipping token_reduction_with_chunking: missing document at {}",
            document_path.display()
        );
        return;
    }
    let config: ExtractionConfig = serde_json::from_str(
        r#"{
  "token_reduction": {
    "mode": "moderate"
  },
  "chunking": {
    "max_chars": 500,
    "max_overlap": 50
  }
}"#,
    )
    .expect("Fixture config should deserialize");

    let result = match kreuzberg::extract_file_sync(&document_path, None, &config) {
        Err(err) => panic!("Extraction failed for token_reduction_with_chunking: {err:?}"),
        Ok(result) => result,
    };

    assertions::assert_expected_mime(&result, &["application/pdf"]);
    assertions::assert_min_content_length(&result, 5);
    assertions::assert_max_content_length(&result, 200);
    assertions::assert_chunks(
        &result,
        &assertions::ChunkAssertions {
            min_count: Some(1),
            max_count: None,
            each_has_content: Some(true),
            each_has_embedding: None,
            each_has_heading_context: None,
            each_has_chunk_type: None,
            content_starts_with_heading: None,
        },
    );
    assertions::assert_content_not_empty(&result);
}
