# TypeScript Packages

Internal directory containing TypeScript source packages for development purposes.

## Contents

- `core/` - `@kreuzberg/core` shared types and utilities
- `tests/` - Shared test utilities

The main npm packages (`@kreuzberg/node`, `@kreuzberg/wasm`) are built from the crates directory.
