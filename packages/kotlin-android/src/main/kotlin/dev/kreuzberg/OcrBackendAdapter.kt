// Generated by alef. Do not edit by hand.
// alef:hash:69999be9c272c5ea97e91f9cb5284c6d81aed5ddd67133adc2dc2ffa2d67ca2f

package dev.kreuzberg

/// Bridge adapter for IOcrBackend implementations.
///
/// Wraps a user-supplied IOcrBackend instance and delegates all method calls.
/// This adapter is used internally by the trait bridge registration layer to ensure
/// proper handling of impl instances through JNI boundaries.
internal class OcrBackendAdapter(private val impl: IOcrBackend) : IOcrBackend {
    // Delegate all interface methods to the wrapped implementation
    override fun name(): String = impl.name()

    override fun version(): String = impl.version()

    override fun initialize() = impl.initialize()

    override fun shutdown() = impl.shutdown()

    override suspend fun processImage(imageBytes: Any, config: String): String {
        return impl.processImage(imageBytes, config).toString()
    }

    override suspend fun processImageFile(path: Any, config: String): String {
        return impl.processImageFile(path, config).toString()
    }

    override fun supportsLanguage(lang: String): String {
        return impl.supportsLanguage(lang).toString()
    }

    override fun backendType(): String {
        return impl.backendType().toString()
    }

    override fun supportedLanguages(): String {
        return impl.supportedLanguages().toString()
    }

    override fun supportsTableDetection(): String {
        return impl.supportsTableDetection().toString()
    }

    override fun supportsDocumentProcessing(): String {
        return impl.supportsDocumentProcessing().toString()
    }

    override suspend fun processDocument(path: Any, config: String): String {
        return impl.processDocument(path, config).toString()
    }


}
