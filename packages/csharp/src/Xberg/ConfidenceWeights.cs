// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:172edec9ba85a11ae0aa02f10ae2bbf6e6f717b3dbc8ebf80571d4582c4d5c3f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Tunable weights for the confidence scoring formula.
///
/// Defaults picked by inspection; callers tune them via config.
/// </summary>
public sealed record ConfidenceWeights
{
    /// <summary>
    /// Weight assigned to `text_coverage`. Default 0.30.
    /// </summary>
    [JsonPropertyName("text_coverage")]
    public float TextCoverage { get; init; } = 0.3f;

    /// <summary>
    /// Weight assigned to `ocr_aggregate` when OCR ran.
    ///
    /// Default 0.30 — folds into `text_coverage` weight when OCR did not run.
    /// </summary>
    [JsonPropertyName("ocr_aggregate")]
    public float OcrAggregate { get; init; } = 0.3f;

    /// <summary>
    /// Weight assigned to `schema_compliance`. Default 0.40.
    /// </summary>
    [JsonPropertyName("schema_compliance")]
    public float SchemaCompliance { get; init; } = 0.4f;


    /// <summary>
    /// Parse a <see cref="ConfidenceWeights"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static ConfidenceWeights FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<ConfidenceWeights>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse ConfidenceWeights from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse ConfidenceWeights from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    public static ConfidenceWeights Default()
    {
        var nativeResult = NativeMethods.ConfidenceWeightsDefault();
        var jsonPtr = NativeMethods.ConfidenceWeightsToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ConfidenceWeightsFree(nativeResult);
        return JsonSerializer.Deserialize<ConfidenceWeights>(json ?? "null", JsonOptions)!;
    }
}
