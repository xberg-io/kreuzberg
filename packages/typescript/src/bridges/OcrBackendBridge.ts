/**
 * Generated by alef. Do not edit by hand.
 * alef:hash:69999be9c272c5ea97e91f9cb5284c6d81aed5ddd67133adc2dc2ffa2d67ca2f
 *
 * TypeScript trait bridge for outbound plugin implementations.
 */

export interface OcrBackendBridge {
  name(): string;
  version(): string;
  initialize(): Promise<void>;
  shutdown(): Promise<void>;
  processImage?(imageBytes: string, config: string): string;
  processImageFile?(path: string, config: string): string;
  supportsLanguage(lang: string): string;
  backendType(): string;
  supportedLanguages(): string;
  supportsTableDetection(): string;
  supportsDocumentProcessing(): string;
  processDocument?(path: string, config: string): string;
}

export class OcrBackendAdapter {
  constructor(private bridge: OcrBackendBridge) {}

  async processImage(imageBytes: string, config: string): Promise<string> {
    try {
      const result = await this.bridge.processImage(imageBytes, config);
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async processImageFile(path: string, config: string): Promise<string> {
    try {
      const result = await this.bridge.processImageFile(path, config);
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async supportsLanguage(lang: string): Promise<string> {
    try {
      const result = await this.bridge.supportsLanguage(lang);
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async backendType(): Promise<string> {
    try {
      const result = await this.bridge.backendType();
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async supportedLanguages(): Promise<string> {
    try {
      const result = await this.bridge.supportedLanguages();
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async supportsTableDetection(): Promise<string> {
    try {
      const result = await this.bridge.supportsTableDetection();
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async supportsDocumentProcessing(): Promise<string> {
    try {
      const result = await this.bridge.supportsDocumentProcessing();
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

  async processDocument(path: string, config: string): Promise<string> {
    try {
      const result = await this.bridge.processDocument(path, config);
      return JSON.stringify({ ok: result });
    } catch (e) {
      return JSON.stringify({ err: String(e) });
    }
  }

}
