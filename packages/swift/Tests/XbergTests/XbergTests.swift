import XCTest

@testable import Xberg

final class XbergTests: XCTestCase {
  func testPlaceholder() throws {
    // Placeholder test so `swift test` has a target to run.
    // Replace or extend with real tests against the Xberg module.
    XCTAssertTrue(true)
  }
}
