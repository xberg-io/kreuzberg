// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0dbb268de39e5c14ada5bfe878d6bf005d50f46c4d0c22df9b2f9ef7f917de33
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import Kreuzberg
import RustBridge

/// E2e tests for category: registry.
final class RegistryTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _testDocs = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent() // <Module>Tests/
            .deletingLastPathComponent() // Tests/
            .deletingLastPathComponent() // swift/
            .deletingLastPathComponent() // packages/
            .deletingLastPathComponent() // <repo root>
            .appendingPathComponent("test_documents")
        if FileManager.default.fileExists(atPath: _testDocs.path) {
            FileManager.default.changeCurrentDirectoryPath(_testDocs.path)
        }
    }

    func testListDocumentExtractors() throws {
        // List document extractors
        let result = try listDocumentExtractors()
    }

    func testListEmbeddingBackends() async throws {
        // List embedding backends
        let configObj = try extractionConfigFromJson("{}")
        let result = try await extractFile(nil, nil, configObj)
    }

    func testListOcrBackends() throws {
        // List OCR backends
        let result = try listOcrBackends()
    }

    func testListPostProcessors() throws {
        // List post-processors
        let result = try listPostProcessors()
    }

    func testListRenderers() async throws {
        // List renderers
        let configObj = try extractionConfigFromJson("{}")
        let result = try await extractFile(nil, nil, configObj)
    }

    func testListValidators() throws {
        // List validators
        let result = try listValidators()
    }

}
