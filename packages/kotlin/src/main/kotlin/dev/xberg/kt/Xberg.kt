// Generated by alef. Do not edit by hand.

package dev.xberg.kt

import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
import java.nio.file.Path
import dev.xberg.Xberg as Bridge

typealias AccelerationConfig = dev.xberg.AccelerationConfig
typealias ContentFilterConfig = dev.xberg.ContentFilterConfig
typealias EmailConfig = dev.xberg.EmailConfig
typealias ExtractionConfig = dev.xberg.ExtractionConfig
typealias FileExtractionConfig = dev.xberg.FileExtractionConfig
typealias BatchBytesItem = dev.xberg.BatchBytesItem
typealias BatchFileItem = dev.xberg.BatchFileItem
typealias ImageExtractionConfig = dev.xberg.ImageExtractionConfig
typealias TokenReductionOptions = dev.xberg.TokenReductionOptions
typealias LanguageDetectionConfig = dev.xberg.LanguageDetectionConfig
typealias HtmlOutputConfig = dev.xberg.HtmlOutputConfig
typealias LayoutDetectionConfig = dev.xberg.LayoutDetectionConfig
typealias LlmConfig = dev.xberg.LlmConfig
typealias StructuredExtractionConfig = dev.xberg.StructuredExtractionConfig
typealias OcrQualityThresholds = dev.xberg.OcrQualityThresholds
typealias OcrPipelineStage = dev.xberg.OcrPipelineStage
typealias OcrPipelineConfig = dev.xberg.OcrPipelineConfig
typealias OcrConfig = dev.xberg.OcrConfig
typealias PageConfig = dev.xberg.PageConfig
typealias PdfConfig = dev.xberg.PdfConfig
typealias HierarchyConfig = dev.xberg.HierarchyConfig
typealias PostProcessorConfig = dev.xberg.PostProcessorConfig
typealias ChunkingConfig = dev.xberg.ChunkingConfig
typealias EmbeddingConfig = dev.xberg.EmbeddingConfig
typealias TreeSitterConfig = dev.xberg.TreeSitterConfig
typealias TreeSitterProcessConfig = dev.xberg.TreeSitterProcessConfig
typealias SupportedFormat = dev.xberg.SupportedFormat
typealias ServerConfig = dev.xberg.ServerConfig
typealias StructuredDataResult = dev.xberg.StructuredDataResult
typealias StreamReader = dev.xberg.StreamReader
typealias ImageOcrResult = dev.xberg.ImageOcrResult
typealias HtmlExtractionResult = dev.xberg.HtmlExtractionResult
typealias ExtractedInlineImage = dev.xberg.ExtractedInlineImage
typealias Drawing = dev.xberg.Drawing
typealias AnchorProperties = dev.xberg.AnchorProperties
typealias HeaderFooter = dev.xberg.HeaderFooter
typealias Note = dev.xberg.Note
typealias PageMarginsPoints = dev.xberg.PageMarginsPoints
typealias StyleDefinition = dev.xberg.StyleDefinition
typealias ResolvedStyle = dev.xberg.ResolvedStyle
typealias TableProperties = dev.xberg.TableProperties
typealias XlsxAppProperties = dev.xberg.XlsxAppProperties
typealias PptxAppProperties = dev.xberg.PptxAppProperties
typealias CustomProperties = dev.xberg.CustomProperties
typealias OdtProperties = dev.xberg.OdtProperties
typealias ZipBombValidator = dev.xberg.ZipBombValidator
typealias EmbeddingBackend = dev.xberg.IEmbeddingBackend
typealias OcrBackend = dev.xberg.IOcrBackend
typealias PostProcessor = dev.xberg.IPostProcessor
typealias Validator = dev.xberg.IValidator
typealias TokenReductionConfig = dev.xberg.TokenReductionConfig
typealias PdfAnnotation = dev.xberg.PdfAnnotation
typealias DjotContent = dev.xberg.DjotContent
typealias FormattedBlock = dev.xberg.FormattedBlock
typealias InlineElement = dev.xberg.InlineElement
typealias DjotImage = dev.xberg.DjotImage
typealias DjotLink = dev.xberg.DjotLink
typealias Footnote = dev.xberg.Footnote
typealias DocumentStructure = dev.xberg.DocumentStructure
typealias DocumentRelationship = dev.xberg.DocumentRelationship
typealias DocumentNode = dev.xberg.DocumentNode
typealias GridCell = dev.xberg.GridCell
typealias TextAnnotation = dev.xberg.TextAnnotation
typealias ExtractionResult = dev.xberg.ExtractionResult
typealias ArchiveEntry = dev.xberg.ArchiveEntry
typealias ProcessingWarning = dev.xberg.ProcessingWarning
typealias LlmUsage = dev.xberg.LlmUsage
typealias Chunk = dev.xberg.Chunk
typealias HeadingContext = dev.xberg.HeadingContext
typealias HeadingLevel = dev.xberg.HeadingLevel
typealias ChunkMetadata = dev.xberg.ChunkMetadata
typealias ExtractedImage = dev.xberg.ExtractedImage
typealias ElementMetadata = dev.xberg.ElementMetadata
typealias Element = dev.xberg.Element
typealias ExcelWorkbook = dev.xberg.ExcelWorkbook
typealias ExcelSheet = dev.xberg.ExcelSheet
typealias XmlExtractionResult = dev.xberg.XmlExtractionResult
typealias TextExtractionResult = dev.xberg.TextExtractionResult
typealias PptxExtractionResult = dev.xberg.PptxExtractionResult
typealias EmailExtractionResult = dev.xberg.EmailExtractionResult
typealias EmailAttachment = dev.xberg.EmailAttachment
typealias OcrExtractionResult = dev.xberg.OcrExtractionResult
typealias OcrTable = dev.xberg.OcrTable
typealias OcrTableBoundingBox = dev.xberg.OcrTableBoundingBox
typealias ImagePreprocessingConfig = dev.xberg.ImagePreprocessingConfig
typealias TesseractConfig = dev.xberg.TesseractConfig
typealias ImagePreprocessingMetadata = dev.xberg.ImagePreprocessingMetadata
typealias Metadata = dev.xberg.Metadata
typealias ExcelMetadata = dev.xberg.ExcelMetadata
typealias EmailMetadata = dev.xberg.EmailMetadata
typealias ArchiveMetadata = dev.xberg.ArchiveMetadata
typealias XmlMetadata = dev.xberg.XmlMetadata
typealias TextMetadata = dev.xberg.TextMetadata
typealias HeaderMetadata = dev.xberg.HeaderMetadata
typealias LinkMetadata = dev.xberg.LinkMetadata
typealias ImageMetadataType = dev.xberg.ImageMetadataType
typealias StructuredData = dev.xberg.StructuredData
typealias HtmlMetadata = dev.xberg.HtmlMetadata
typealias OcrMetadata = dev.xberg.OcrMetadata
typealias ErrorMetadata = dev.xberg.ErrorMetadata
typealias PptxMetadata = dev.xberg.PptxMetadata
typealias DocxMetadata = dev.xberg.DocxMetadata
typealias CsvMetadata = dev.xberg.CsvMetadata
typealias BibtexMetadata = dev.xberg.BibtexMetadata
typealias CitationMetadata = dev.xberg.CitationMetadata
typealias YearRange = dev.xberg.YearRange
typealias FictionBookMetadata = dev.xberg.FictionBookMetadata
typealias DbfMetadata = dev.xberg.DbfMetadata
typealias DbfFieldInfo = dev.xberg.DbfFieldInfo
typealias JatsMetadata = dev.xberg.JatsMetadata
typealias ContributorRole = dev.xberg.ContributorRole
typealias EpubMetadata = dev.xberg.EpubMetadata
typealias PstMetadata = dev.xberg.PstMetadata
typealias OcrConfidence = dev.xberg.OcrConfidence
typealias OcrRotation = dev.xberg.OcrRotation
typealias OcrElement = dev.xberg.OcrElement
typealias OcrElementConfig = dev.xberg.OcrElementConfig
typealias PageStructure = dev.xberg.PageStructure
typealias PageBoundary = dev.xberg.PageBoundary
typealias PageInfo = dev.xberg.PageInfo
typealias PageContent = dev.xberg.PageContent
typealias LayoutRegion = dev.xberg.LayoutRegion
typealias PageHierarchy = dev.xberg.PageHierarchy
typealias HierarchicalBlock = dev.xberg.HierarchicalBlock
typealias Uri = dev.xberg.Uri
typealias StringBufferPool = dev.xberg.StringBufferPool
typealias ByteBufferPool = dev.xberg.ByteBufferPool
typealias TracingLayer = dev.xberg.TracingLayer
typealias ApiDoc = dev.xberg.ApiDoc
typealias HealthResponse = dev.xberg.HealthResponse
typealias InfoResponse = dev.xberg.InfoResponse
typealias ExtractResponse = dev.xberg.ExtractResponse
typealias ApiState = dev.xberg.ApiState
typealias CacheStatsResponse = dev.xberg.CacheStatsResponse
typealias CacheClearResponse = dev.xberg.CacheClearResponse
typealias EmbedRequest = dev.xberg.EmbedRequest
typealias EmbedResponse = dev.xberg.EmbedResponse
typealias ChunkRequest = dev.xberg.ChunkRequest
typealias ChunkResponse = dev.xberg.ChunkResponse
typealias VersionResponse = dev.xberg.VersionResponse
typealias DetectResponse = dev.xberg.DetectResponse
typealias ManifestEntryResponse = dev.xberg.ManifestEntryResponse
typealias ManifestResponse = dev.xberg.ManifestResponse
typealias WarmRequest = dev.xberg.WarmRequest
typealias WarmResponse = dev.xberg.WarmResponse
typealias StructuredExtractionResponse = dev.xberg.StructuredExtractionResponse
typealias OpenWebDocumentResponse = dev.xberg.OpenWebDocumentResponse
typealias DoclingCompatResponse = dev.xberg.DoclingCompatResponse
typealias ExtractFileParams = dev.xberg.ExtractFileParams
typealias ExtractBytesParams = dev.xberg.ExtractBytesParams
typealias BatchExtractFilesParams = dev.xberg.BatchExtractFilesParams
typealias DetectMimeTypeParams = dev.xberg.DetectMimeTypeParams
typealias CacheWarmParams = dev.xberg.CacheWarmParams
typealias EmbedTextParams = dev.xberg.EmbedTextParams
typealias ExtractStructuredParams = dev.xberg.ExtractStructuredParams
typealias ChunkTextParams = dev.xberg.ChunkTextParams
typealias DetectedBoundary = dev.xberg.DetectedBoundary
typealias ChunkingResult = dev.xberg.ChunkingResult
typealias MergedChunk = dev.xberg.MergedChunk
typealias YakeParams = dev.xberg.YakeParams
typealias RakeParams = dev.xberg.RakeParams
typealias KeywordConfig = dev.xberg.KeywordConfig
typealias Keyword = dev.xberg.Keyword
typealias OcrCacheStats = dev.xberg.OcrCacheStats
typealias RecognizedTable = dev.xberg.RecognizedTable
typealias TessdataManager = dev.xberg.TessdataManager
typealias PaddleOcrConfig = dev.xberg.PaddleOcrConfig
typealias ModelPaths = dev.xberg.ModelPaths
typealias OrientationResult = dev.xberg.OrientationResult
typealias BBox = dev.xberg.BBox
typealias LayoutDetection = dev.xberg.LayoutDetection
typealias DetectionResult = dev.xberg.DetectionResult
typealias EmbeddedFile = dev.xberg.EmbeddedFile
typealias PdfImage = dev.xberg.PdfImage
typealias PageLayoutResult = dev.xberg.PageLayoutResult
typealias PageTiming = dev.xberg.PageTiming
typealias CommonPdfMetadata = dev.xberg.CommonPdfMetadata
typealias PdfUnifiedExtractionResult = dev.xberg.PdfUnifiedExtractionResult

typealias ExecutionProviderType = dev.xberg.ExecutionProviderType
typealias OutputFormat = dev.xberg.OutputFormat
typealias HtmlTheme = dev.xberg.HtmlTheme
typealias TableModel = dev.xberg.TableModel
typealias PdfBackend = dev.xberg.PdfBackend
typealias ChunkerType = dev.xberg.ChunkerType
typealias ChunkSizing = dev.xberg.ChunkSizing
typealias EmbeddingModelType = dev.xberg.EmbeddingModelType
typealias CodeContentMode = dev.xberg.CodeContentMode
typealias FracType = dev.xberg.FracType
typealias OcrBackendType = dev.xberg.OcrBackendType
typealias ProcessingStage = dev.xberg.ProcessingStage
typealias ReductionLevel = dev.xberg.ReductionLevel
typealias PdfAnnotationType = dev.xberg.PdfAnnotationType
typealias BlockType = dev.xberg.BlockType
typealias InlineType = dev.xberg.InlineType
typealias RelationshipKind = dev.xberg.RelationshipKind
typealias ContentLayer = dev.xberg.ContentLayer
typealias NodeContent = dev.xberg.NodeContent
typealias AnnotationKind = dev.xberg.AnnotationKind
typealias ExtractionMethod = dev.xberg.ExtractionMethod
typealias ChunkType = dev.xberg.ChunkType
typealias ImageKind = dev.xberg.ImageKind
typealias ResultFormat = dev.xberg.ResultFormat
typealias ElementType = dev.xberg.ElementType
typealias FormatMetadata = dev.xberg.FormatMetadata
typealias TextDirection = dev.xberg.TextDirection
typealias LinkType = dev.xberg.LinkType
typealias ImageType = dev.xberg.ImageType
typealias StructuredDataType = dev.xberg.StructuredDataType
typealias OcrBoundingGeometry = dev.xberg.OcrBoundingGeometry
typealias OcrElementLevel = dev.xberg.OcrElementLevel
typealias PageUnitType = dev.xberg.PageUnitType
typealias UriKind = dev.xberg.UriKind
typealias PoolError = dev.xberg.PoolError
typealias KeywordAlgorithm = dev.xberg.KeywordAlgorithm
typealias PSMMode = dev.xberg.PSMMode
typealias PaddleLanguage = dev.xberg.PaddleLanguage
typealias LayoutClass = dev.xberg.LayoutClass

typealias XbergErrorException = dev.xberg.XbergErrorException

object Xberg {
    /**
     * Extract content from a byte array.
     *
     * This is the main entry point for in-memory extraction. It performs the following steps:
     * 1. Validate MIME type
     * 2. Handle legacy format conversion if needed
     * 3. Select appropriate extractor from registry
     * 4. Extract content
     * 5. Run post-processing pipeline
     *
     * **Returns:**
     *
     * An `ExtractionResult` containing the extracted content and metadata.
     *
     * **Errors:**
     *
     * Returns `XbergError.Validation` if MIME type is invalid.
     * Returns `XbergError.UnsupportedFormat` if MIME type is not supported.
     */
    suspend fun extractBytes(
        content: ByteArray,
        mimeType: String,
        config: ExtractionConfig,
    ): ExtractionResult =
        withContext(Dispatchers.IO) {
            Bridge.extractBytes(content, mimeType, config)
        }

    /**
     * Extract content from a file.
     *
     * This is the main entry point for file-based extraction. It performs the following steps:
     * 1. Check cache for existing result (if caching enabled)
     * 2. Detect or validate MIME type
     * 3. Select appropriate extractor from registry
     * 4. Extract content
     * 5. Run post-processing pipeline
     * 6. Store result in cache (if caching enabled)
     *
     * **Returns:**
     *
     * An `ExtractionResult` containing the extracted content and metadata.
     *
     * **Errors:**
     *
     * Returns `XbergError.Io` if the file doesn't exist (NotFound) or for other file I/O errors.
     * Returns `XbergError.UnsupportedFormat` if MIME type is not supported.
     */
    suspend fun extractFile(
        path: Path,
        mimeType: String?,
        config: ExtractionConfig,
    ): ExtractionResult =
        withContext(Dispatchers.IO) {
            Bridge.extractFile(path, mimeType, config)
        }

    /**
     * Synchronous wrapper for `extract_file`.
     *
     * This is a convenience function that blocks the current thread until extraction completes.
     * For async code, use `extract_file` directly.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating
     * a new runtime per call. Always uses the global runtime to avoid nested runtime issues.
     *
     * This function is only available with the `tokio-runtime` feature. For WASM targets,
     * use a truly synchronous extraction approach instead.
     */
    fun extractFileSync(
        path: Path,
        mimeType: String?,
        config: ExtractionConfig,
    ): ExtractionResult = Bridge.extractFileSync(path, mimeType, config)

    /**
     * Synchronous wrapper for `extract_bytes`.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating
     * a new runtime per call.
     *
     * With the `tokio-runtime` feature, this blocks the current thread using the global
     * Tokio runtime. Without it (WASM), this calls a truly synchronous implementation.
     */
    fun extractBytesSync(
        content: ByteArray,
        mimeType: String,
        config: ExtractionConfig,
    ): ExtractionResult = Bridge.extractBytesSync(content, mimeType, config)

    /**
     * Synchronous wrapper for `batch_extract_file`.
     *
     * Uses the global Tokio runtime for optimal performance.
     * Only available with `tokio-runtime` (WASM has no filesystem).
     */
    fun batchExtractFileSync(
        items: List<BatchFileItem>,
        config: ExtractionConfig,
    ): List<ExtractionResult> = Bridge.batchExtractFileSync(items, config)

    /**
     * Synchronous wrapper for `batch_extract_bytes`.
     *
     * Uses the global Tokio runtime for optimal performance.
     * With the `tokio-runtime` feature, this blocks the current thread using the global
     * Tokio runtime. Without it (WASM), this calls a truly synchronous implementation
     * that iterates through items and calls `extract_bytes_sync()`.
     */
    fun batchExtractBytesSync(
        items: List<BatchBytesItem>,
        config: ExtractionConfig,
    ): List<ExtractionResult> = Bridge.batchExtractBytesSync(items, config)

    /**
     * Extract content from multiple files concurrently.
     *
     * This function processes multiple files in parallel, automatically managing
     * concurrency to prevent resource exhaustion. The concurrency limit can be
     * configured via `ExtractionConfig.max_concurrent_extractions` or defaults
     * to `(num_cpus * 1.5).ceil()`.
     *
     * Each file can optionally specify a `FileExtractionConfig` that overrides specific
     * fields from the batch-level `config`. Pass `null` for a file to use the batch defaults.
     * Batch-level settings like `max_concurrent_extractions` and `use_cache` are always
     * taken from the batch-level `config`.
     *
     *   per-file configuration overrides.
     * * `config` - Batch-level extraction configuration (provides defaults and batch settings)
     *
     * **Returns:**
     *
     * A vector of `ExtractionResult` in the same order as the input items.
     *
     * **Errors:**
     *
     * Individual file errors are captured in the result metadata. System errors
     * (IO, RuntimeError equivalents) will bubble up and fail the entire batch.
     *
     * Simple usage with no per-file overrides:
     *
     *
     * Per-file configuration overrides:
     */
    suspend fun batchExtractFile(
        items: List<BatchFileItem>,
        config: ExtractionConfig,
    ): List<ExtractionResult> =
        withContext(Dispatchers.IO) {
            Bridge.batchExtractFile(items, config)
        }

    /**
     * Extract content from multiple byte arrays concurrently.
     *
     * This function processes multiple byte arrays in parallel, automatically managing
     * concurrency to prevent resource exhaustion. The concurrency limit can be
     * configured via `ExtractionConfig.max_concurrent_extractions` or defaults
     * to `(num_cpus * 1.5).ceil()`.
     *
     * Each item can optionally specify a `FileExtractionConfig` that overrides specific
     * fields from the batch-level `config`. Pass `null` as the config to use
     * the batch-level defaults for that item.
     *
     *   MIME type, and optional per-item configuration overrides.
     * * `config` - Batch-level extraction configuration
     *
     * **Returns:**
     *
     * A vector of `ExtractionResult` in the same order as the input items.
     *
     * Simple usage with no per-item overrides:
     *
     *
     * Per-item configuration overrides:
     */
    suspend fun batchExtractBytes(
        items: List<BatchBytesItem>,
        config: ExtractionConfig,
    ): List<ExtractionResult> =
        withContext(Dispatchers.IO) {
            Bridge.batchExtractBytes(items, config)
        }

    /**
     * Detect MIME type from raw file bytes.
     *
     * Uses magic byte signatures to detect file type from content.
     * Falls back to `infer` crate for comprehensive detection.
     *
     * For ZIP-based files, inspects contents to distinguish Office Open XML
     * formats (DOCX, XLSX, PPTX) from plain ZIP archives.
     *
     * **Returns:**
     *
     * The detected MIME type string.
     *
     * **Errors:**
     *
     * Returns `XbergError.UnsupportedFormat` if MIME type cannot be determined.
     */
    fun detectMimeTypeFromBytes(content: ByteArray): String = Bridge.detectMimeTypeFromBytes(content)

    /**
     * Get file extensions for a given MIME type.
     *
     * Returns all known file extensions that map to the specified MIME type.
     *
     * **Returns:**
     *
     * A vector of file extensions (without leading dot) for the MIME type.
     */
    fun getExtensionsForMime(mimeType: String): List<String> = Bridge.getExtensionsForMime(mimeType)

    /**
     * List names of all registered document extractors.
     *
     * Re-exported at the crate root as `list_document_extractors`.
     */
    fun listExtractors(): List<String> = Bridge.listExtractors()

    /**
     * List all registered OCR backends.
     *
     * Returns the names of all OCR backends currently registered in the global registry.
     *
     * **Returns:**
     *
     * A vector of OCR backend names.
     */
    fun listOcrBackends(): List<String> = Bridge.listOcrBackends()

    /**
     * Clear all OCR backends from the global registry.
     *
     * Removes all OCR backends and calls their `shutdown()` methods.
     *
     * **Returns:**
     *
     * - `Ok(())` if all backends were cleared successfully
     * - `Err(...)` if any shutdown method failed
     */
    fun clearOcrBackends() {
        Bridge.clearOcrBackends()
    }

    /**
     * List all registered post-processor names.
     *
     * Returns a vector of all post-processor names currently registered in the
     * global registry.
     *
     * **Returns:**
     *
     * - `Ok(Vec<String>)` - Vector of post-processor names
     * - `Err(...)` if the registry lock is poisoned
     */
    fun listPostProcessors(): List<String> = Bridge.listPostProcessors()

    /**
     * Remove all registered post-processors.
     */
    fun clearPostProcessors() {
        Bridge.clearPostProcessors()
    }

    /**
     * List names of all registered validators.
     */
    fun listValidators(): List<String> = Bridge.listValidators()

    /**
     * Remove all registered validators.
     */
    fun clearValidators() {
        Bridge.clearValidators()
    }

    /**
     * Render a single PDF page to a PNG-encoded byte buffer.
     *
     * **Errors:**
     *
     * Returns an error if the PDF is invalid, the page index is out of bounds,
     * or if the page fails to render.
     */
    fun renderPdfPageToPng(
        pdfBytes: ByteArray,
        pageIndex: Long,
        dpi: Int?,
        password: String?,
    ): ByteArray = Bridge.renderPdfPageToPng(pdfBytes, pageIndex, dpi, password)

    /**
     * Detect the MIME type of a file at the given path.
     *
     * Uses the file extension and optionally the file content to determine the MIME type.
     * Set `check_exists` to `true` to verify the file exists before detection.
     */
    fun detectMimeType(
        path: String,
        checkExists: Boolean,
    ): String = Bridge.detectMimeType(path, checkExists)

    /**
     * Embed a list of texts using the configured embedding model.
     *
     * Returns a 2D vector where each inner vector is the embedding for the corresponding text.
     */
    fun embedTexts(
        texts: List<String>,
        config: EmbeddingConfig?,
    ): List<List<Float>> = Bridge.embedTexts(texts, config)

    /**
     * Get an embedding preset by name.
     *
     * Returns `null` if no preset with the given name exists.
     */
    fun getEmbeddingPreset(name: String): String? = Bridge.getEmbeddingPreset(name)

    /**
     * List the names of all available embedding presets.
     */
    fun listEmbeddingPresets(): List<String> = Bridge.listEmbeddingPresets()
}
