```bash title="Bash"
# Start server via Python CLI proxy
python -m xberg serve

# Specify host and port
python -m xberg serve --host 0.0.0.0 --port 3000

# With custom config
python -m xberg serve --config production.toml

```
