```bash title="Bash"
# Start server (default: 127.0.0.1:8000)
xberg serve

# Specify host and port
xberg serve --host 0.0.0.0 --port 3000

# With custom config
xberg serve --config production.toml

```
