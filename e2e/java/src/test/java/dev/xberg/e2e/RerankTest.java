// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:172edec9ba85a11ae0aa02f10ae2bbf6e6f717b3dbc8ebf80571d4582c4d5c3f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package dev.xberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.xberg.Xberg;
import dev.xberg.ExtractionConfig;
import dev.xberg.RerankerConfig;
import java.util.Optional;
import dev.xberg.JsonUtil;

/** E2e tests for category: rerank. */
public class RerankTest {

    @Test
    void testRerankBasic() throws Exception {
        // rerank: plugin model with no backend registered returns an error
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var config = JsonUtil.fromJson("{\"model\":{\"name\":\"rerank_basic\",\"type\":\"plugin\"}}", RerankerConfig.class);

            Xberg.rerankAsync("What is machine learning?", java.util.Arrays.asList(JsonUtil.fromJson("\"Machine learning is a subset of artificial intelligence focused on learning from data.\"", String.class), JsonUtil.fromJson("\"Cats are independent animals that prefer to play alone.\"", String.class), JsonUtil.fromJson("\"Deep learning uses neural networks with multiple layers.\"", String.class)), config);
        });

    }


    @Test
    void testRerankEmptyDocuments() throws Exception {
        // rerank: empty documents list returns empty result
        var config = JsonUtil.fromJson("{\"model\":{\"name\":\"rerank_empty_documents\",\"type\":\"plugin\"}}", RerankerConfig.class);

        var result = Xberg.rerankAsync("test query", java.util.List.of(), config);


    }


    @Test
    void testRerankTopK() throws Exception {
        // rerank: plugin model with no backend registered returns an error (top_k variant)
        // Wrap setup_lines + call_expr inside the lambda so error fixtures
        // catch failures at *any* step — including `<Type>.fromJson(...)`
        // calls that throw on malformed JSON (e.g. error fixtures with an
        // invalid enum value like `"purpose":"invalid-purpose"`). Mirrors
        // the C# `Assert.ThrowsAnyAsync(() => client.X(Type.FromJson(...)))`
        // pattern.
        assertThrows(Exception.class, () -> {
        var config = JsonUtil.fromJson("{\"model\":{\"name\":\"rerank_top_k\",\"type\":\"plugin\"},\"top_k\":2}", RerankerConfig.class);

            Xberg.rerankAsync("machine learning", java.util.Arrays.asList(JsonUtil.fromJson("\"Machine learning is a subset of artificial intelligence focused on learning from data.\"", String.class), JsonUtil.fromJson("\"Deep learning uses neural networks with multiple layers.\"", String.class), JsonUtil.fromJson("\"Classical statistics predates modern machine learning.\"", String.class), JsonUtil.fromJson("\"Photosynthesis is the process by which plants convert sunlight to energy.\"", String.class)), config);
        });

    }

}
