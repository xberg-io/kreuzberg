// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5fab49d56ca7c077be12d57a7f43f831cc66d0ae09bb86759d964309df83ac43
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;

/** E2e tests for category: rendering. */
class RenderingTest {
    @Test
    void testConvertHtmlBasic() throws Exception {
        // Convert basic HTML to Markdown format
        var result = Kreuzberg.convertHtmlToMarkdown("<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>");
        assertTrue(result.contains("Title"), "expected to contain: " + "Title");
    }

    @Test
    void testConvertHtmlComplex() throws Exception {
        // Convert complex HTML to markdown
        var result = Kreuzberg.convertHtmlToMarkdown("<h1>Title</h1><p>Text with <strong>bold</strong> and <a href=\"https://example.com\">link</a>.</p>");
        assertTrue(result.length() >= 20, "expected length >= 20");
    }

    @Test
    void testConvertHtmlEmpty() throws Exception {
        // Convert empty HTML to markdown
        var result = Kreuzberg.convertHtmlToMarkdown("");
    }

    @Test
    void testConvertHtmlTable() throws Exception {
        // Convert HTML table to Markdown table format
        var result = Kreuzberg.convertHtmlToMarkdown("<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>");
        assertTrue(result.contains("|"), "expected to contain: " + "|");
    }

    @Test
    void testDjotToHtmlBasic() throws Exception {
        // Convert Djot format to HTML
        var result = Kreuzberg.djotToHtml("# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2");
        assertTrue(result.length() >= 10, "expected length >= 10");
    }

    @Test
    void testDjotToHtmlComplex() throws Exception {
        // Complex djot to HTML
        var result = Kreuzberg.djotToHtml("# Heading\n\nParagraph.\n\n- Item one\n- Item two");
        assertTrue(result.length() >= 10, "expected length >= 10");
    }

    @Test
    void testDjotToHtmlEmpty() throws Exception {
        // Convert empty djot to HTML
        var result = Kreuzberg.djotToHtml("");
    }

    @Test
    void testRenderDjotBasic() throws Exception {
        // Render HTML document to Djot format
        var result = Kreuzberg.renderDjotStr("<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>");
        assertTrue(result.length() >= 5, "expected length >= 5");
    }

    @Test
    void testRenderDjotFormatted() throws Exception {
        // Render HTML to djot
        var result = Kreuzberg.renderDjotStr("<h2>Section</h2><p>Text with <code>code</code>.</p>");
    }

    @Test
    void testRenderHtmlBasic() throws Exception {
        // Render a simple HTML document to formatted output
        var result = Kreuzberg.renderHtmlStr("<h1>Test Document</h1><p>This is a paragraph with some test content.</p>");
        assertTrue(result.length() >= 5, "expected length >= 5");
    }

    @Test
    void testRenderHtmlStyled() throws Exception {
        // Render HTML preserving structure
        var result = Kreuzberg.renderHtmlStr("<h1>Heading</h1><p><strong>bold</strong></p>");
        assertTrue(result.contains("Heading"), "expected to contain: " + "Heading");
    }

    @Test
    void testRenderJsonBasic() throws Exception {
        // Render HTML document to JSON format
        var result = Kreuzberg.renderJsonStr("<h1>Structured Data</h1><p>Content for JSON rendering.</p>");
        assertTrue(result.length() >= 5, "expected length >= 5");
    }

    @Test
    void testRenderJsonComplex() throws Exception {
        // Render HTML to JSON
        var result = Kreuzberg.renderJsonStr("<h1>Title</h1><p>Text.</p>");
        assertTrue(result.length() >= 5, "expected length >= 5");
    }

    @Test
    void testRenderMarkdownBasic() throws Exception {
        // Render HTML document to Markdown format
        var result = Kreuzberg.renderMarkdownStr("<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>");
        assertTrue(result.length() >= 5, "expected length >= 5");
    }

    @Test
    void testRenderMarkdownComplex() throws Exception {
        // Render complex HTML to markdown
        var result = Kreuzberg.renderMarkdownStr("<h1>Title</h1><ul><li>A</li><li>B</li></ul>");
        assertTrue(result.length() >= 10, "expected length >= 10");
    }

    @Test
    void testRenderPlainBasic() throws Exception {
        // Render HTML document to plain text format
        var result = Kreuzberg.renderPlainStr("<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>");
        assertTrue(result.length() >= 5, "expected length >= 5");
    }

    @Test
    void testRenderPlainEmpty() throws Exception {
        // Render empty HTML
        var result = Kreuzberg.renderPlainStr("");
    }

}
