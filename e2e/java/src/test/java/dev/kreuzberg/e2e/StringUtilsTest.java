// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d198d4032ff3ac0b71ad30266fe09b7aba8a7a6d8c950808017e0eea36140090
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;

/** E2e tests for category: string_utils. */
class StringUtilsTest {
    @Test
    void testCamelToSnakeBasic() throws Exception {
        // Convert camelCase to snake_case
        var result = Kreuzberg.camelToSnake("myVariableName");
        assertEquals("my_variable_name", result.result().trim());
    }

    @Test
    void testCamelToSnakeSingle() throws Exception {
        // Convert single word (no camelCase) remains unchanged
        var result = Kreuzberg.camelToSnake("hello");
        assertEquals("hello", result.result().trim());
    }

    @Test
    void testSanitizeFilenameBasic() throws Exception {
        // Sanitize path traversal attempts in filename
        var result = Kreuzberg.sanitizeFilename("../../etc/passwd");
        assertTrue(result.result().length() >= 1, "expected length >= 1");
    }

    @Test
    void testSanitizeFilenameSpecial() throws Exception {
        // Sanitize special characters in filename
        var result = Kreuzberg.sanitizeFilename("file<>name|test.pdf");
        assertTrue(result.result().length() >= 1, "expected length >= 1");
    }

    @Test
    void testSanitizePathBasic() throws Exception {
        // Sanitize path traversal in file path
        var result = Kreuzberg.sanitizePath("../../../secret/file.txt");
        assertTrue(result.result().length() >= 1, "expected length >= 1");
    }

    @Test
    void testSanitizePathSpaces() throws Exception {
        // Sanitize path with spaces
        var result = Kreuzberg.sanitizePath("my folder/file.txt");
    }

    @Test
    void testSanitizePathTraversal() throws Exception {
        // Sanitize path traversal
        var result = Kreuzberg.sanitizePath("../../../etc/passwd");
    }

    @Test
    void testSnakeToCamelBasic() throws Exception {
        // Convert snake_case to camelCase
        var result = Kreuzberg.snakeToCamel("my_variable_name");
        assertEquals("myVariableName", result.result().trim());
    }

    @Test
    void testSnakeToCamelSingle() throws Exception {
        // Convert single word remains unchanged
        var result = Kreuzberg.snakeToCamel("hello");
        assertEquals("hello", result.result().trim());
    }

}
