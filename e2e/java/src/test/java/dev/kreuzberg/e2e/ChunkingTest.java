// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5c7c194f099393bbaf86e2ae35d7637cd4f65f6d1f31f1f1bbbdc811bfb65555
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ChunkingConfig;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: chunking. */
class ChunkingTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testChunkBatchBasic() throws Exception {
        // Batch chunking of multiple texts in a single call
        var config = MAPPER.readValue("{\"max_characters\":100,\"max_overlap\":10}", ChunkingConfig.class);
        var result = Kreuzberg.chunkTextsBatch(java.util.List.of("Hello world. This is text one.", "Goodbye world. This is text two."), config);
    }

    @Test
    void testChunkTextBasic() throws Exception {
        // Basic text chunking with small chunk size and overlap
        var config = MAPPER.readValue("{\"max_characters\":50,\"max_overlap\":10}", ChunkingConfig.class);
        var result = Kreuzberg.chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", config);
    }

    @Test
    void testChunkTextLarge() throws Exception {
        // Text chunking with large chunk size to test handling of long content
        var config = MAPPER.readValue("{\"max_characters\":1000,\"max_overlap\":10}", ChunkingConfig.class);
        var result = Kreuzberg.chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", config);
    }

    @Test
    void testChunkTextNoOverlap() throws Exception {
        // Text chunking with no overlap between chunks
        var config = MAPPER.readValue("{\"max_characters\":50,\"max_overlap\":0}", ChunkingConfig.class);
        var result = Kreuzberg.chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", config);
    }

    @Test
    void testChunkTextOverlapExceeds() throws Exception {
        // chunk_text overlap >= max
        var config = MAPPER.readValue("{\"max_characters\":50,\"overlap\":60}", ChunkingConfig.class);
        assertThrows(Exception.class, () -> Kreuzberg.chunkText("Some text.", config));
    }

    @Test
    void testChunkTextYaml() throws Exception {
        // Chunk with YAML chunker
        var config = MAPPER.readValue("{\"chunker_type\":\"yaml\",\"max_characters\":50}", ChunkingConfig.class);
        var result = Kreuzberg.chunkText("key1: value1\nkey2: value2\n---\nkey3: value3", config);
    }

    @Test
    void testChunkTextZeroMax() throws Exception {
        // chunk_text max_characters zero
        var config = MAPPER.readValue("{\"max_characters\":0}", ChunkingConfig.class);
        assertThrows(Exception.class, () -> Kreuzberg.chunkText("Some text.", config));
    }

    @Test
    void testChunkTextsBatchMultiple() throws Exception {
        // Batch chunk multiple texts
        var config = MAPPER.readValue("{\"max_characters\":100}", ChunkingConfig.class);
        var result = Kreuzberg.chunkTextsBatch(java.util.List.of("First doc text.", "Second doc text."), config);
    }

}
