// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7b1026d51e49a2f9fee6df2c47a932e31fafd1942f594b8f4ed3c995bc2107ca
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.ExtractionConfig;

/** E2e tests for category: serialization. */
class SerializationTest {

    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module());
    @Test
    void testSerializeJsonBasic() throws Exception {
        // Serialize extraction result to JSON
        var config = MAPPER.readValue("{}", ExtractionConfig.class);
        var result = Kreuzberg.extractFileToJson(java.nio.file.Path.of("test_documents/text/plain.txt"), config);
        assertTrue(result.length() >= 1, "expected length >= 1");
    }

    @Test
    void testSerializeJsonPdf() throws Exception {
        // Serialize PDF extraction to JSON
        var config = MAPPER.readValue("{}", ExtractionConfig.class);
        var result = Kreuzberg.extractFileToJson(java.nio.file.Path.of("test_documents/pdf/sample.pdf"), config);
        assertTrue(result.contains("content"), "expected to contain: " + "content");
    }

    @Test
    void testSerializeToonBasic() throws Exception {
        // Serialize extraction result to TOON format
        var config = MAPPER.readValue("{}", ExtractionConfig.class);
        var result = Kreuzberg.extractFileToToon(java.nio.file.Path.of("test_documents/text/plain.txt"), config);
        assertTrue(result.length() >= 1, "expected length >= 1");
    }

    @Test
    void testSerializeToonPdf() throws Exception {
        // Serialize PDF extraction to TOON
        var config = MAPPER.readValue("{}", ExtractionConfig.class);
        var result = Kreuzberg.extractFileToToon(java.nio.file.Path.of("test_documents/pdf/sample.pdf"), config);
        assertTrue(result.length() >= 10, "expected length >= 10");
    }

}
