// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8c5769a9cd38ee201f77ed2fe5f4d9de40f7979c3187001ab4e3d10b83fee2ed
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'dart:io';
import 'package:kreuzberg/kreuzberg.dart';
import 'package:kreuzberg/src/kreuzberg_bridge_generated/frb_generated.dart' show RustLib;

// E2e tests for category: contract
void main() {
  setUpAll(() async {
    await RustLib.init();
    final _testDocs = Platform.environment['FIXTURES_DIR'] ?? '../../test_documents';
    final _dir = Directory(_testDocs);
    if (_dir.existsSync()) Directory.current = _dir;
  });

  test('Tests document structure with DOCX heading-driven nesting', () async {
    final result = await KreuzbergBridge.extractBytesSync(File('docx/fake.docx').readAsBytesSync(), mimeType: 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', ExtractionConfig(useCache: true, enableQualityProcessing: true, forceOcr: false, disableOcr: false, resultFormat: ResultFormat.unified, outputFormat: OutputFormat.plain(), includeDocumentStructure: true, maxArchiveDepth: 3));
    expect(result.mimeType, equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.document, isNotEmpty);
    expect(result.document.nodes.length, greaterThanOrEqualTo(1));
  });

  test('Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions', () async {
    final result = await KreuzbergBridge.extractBytesSync(File('pdf/fake_memo.pdf').readAsBytesSync(), mimeType: 'application/pdf', ExtractionConfig(useCache: true, enableQualityProcessing: true, forceOcr: false, disableOcr: false, resultFormat: ResultFormat.unified, outputFormat: OutputFormat.plain(), includeDocumentStructure: false, maxArchiveDepth: 3));
    expect(result.mimeType, equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Tests archive extraction with custom security limits', () async {
    final result = await KreuzbergBridge.extractBytesSync(File('archives/documents.zip').readAsBytesSync(), mimeType: 'application/zip', ExtractionConfig(useCache: true, enableQualityProcessing: true, forceOcr: false, disableOcr: false, resultFormat: ResultFormat.unified, outputFormat: OutputFormat.plain(), includeDocumentStructure: false, maxArchiveDepth: 3));
    expect(result.mimeType.contains('application/zip') || result.mimeType.contains('application/x-zip-compressed'), isTrue);
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Tests markdown output format via bytes extraction API', () async {
    final result = await KreuzbergBridge.extractBytesSync(File('pdf/fake_memo.pdf').readAsBytesSync(), mimeType: 'application/pdf', ExtractionConfig(useCache: true, enableQualityProcessing: true, forceOcr: false, disableOcr: false, resultFormat: ResultFormat.unified, outputFormat: OutputFormat.plain(), includeDocumentStructure: false, maxArchiveDepth: 3));
    expect(result.mimeType, equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.metadata.outputFormat, equals('markdown'));
  });

  test('Tests Markdown output format', () async {
    final result = await KreuzbergBridge.extractBytesSync(File('pdf/fake_memo.pdf').readAsBytesSync(), mimeType: 'application/pdf', ExtractionConfig(useCache: true, enableQualityProcessing: true, forceOcr: false, disableOcr: false, resultFormat: ResultFormat.unified, outputFormat: OutputFormat.plain(), includeDocumentStructure: false, maxArchiveDepth: 3));
    expect(result.mimeType, equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.metadata.outputFormat, equals('markdown'));
  });

}
