# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:4bf4e64a1b7dfce75f80bdb8d14a23f059b7a4418ab63e8539f38170269279e8
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: render

test_that("pdf_page_count_basic: Count pages in a small PDF without rendering them", {
  result <- pdf_page_count(pdf_bytes = "pdf/tiny.pdf")
  expect_true(result >= 1)
})

test_that("render_custom_dpi: Render PDF page with custom DPI", {
  result <- render_page(path = "pdf/tiny.pdf")
  expect_true(result$is_png)
  expect_true(result$byte_length >= 50)
})

test_that("render_high_dpi: Render PDF page with high DPI setting (300 DPI)", {
  result <- render_page(path = "pdf/fake_memo.pdf", page = 0, config = list("dpi" = 300))
})

test_that("render_iterator: Iterate all PDF pages as PNG images", {
  result <- render_page(path = "pdf/tiny.pdf")
  expect_true(result$is_png)
  expect_true(result$page_count >= 1)
})

test_that("render_multi_page: Render a page from a multi-page PDF document", {
  result <- render_page(path = "pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf", page = 0)
  expect_true(nchar(result$result) >= 1000)
})

test_that("render_output_djot: Extract PDF with Djot output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "djot"))
  expect_equal(trimws(result$output_format), "djot")
  expect_true(nchar(result$content) >= 10)
})

test_that("render_output_html: Extract PDF with HTML output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "html"))
  expect_equal(trimws(result$output_format), "html")
  expect_true(any(sapply(c("<", ">", "</"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("render_output_markdown: Extract PDF with Markdown output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "markdown"))
  expect_equal(trimws(result$output_format), "markdown")
  expect_true(any(sapply(c("#", "**", "*"), function(v) grepl(v, result$content, fixed = TRUE))))
})

test_that("render_output_plain: Extract PDF with plain text output format", {
  result <- extract_file_sync(path = "pdf/fake_memo.pdf", config = list("output_format" = "plain"))
  expect_equal(trimws(result$output_format), "plain")
  expect_true(nchar(result$content) >= 50)
})

test_that("render_single_page: Render a single PDF page to PNG", {
  result <- render_page(path = "pdf/tiny.pdf")
  expect_true(result$is_png)
  expect_true(result$byte_length >= 100)
})
