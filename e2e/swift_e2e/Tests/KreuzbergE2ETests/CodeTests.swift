// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:df949a23ed01418d22789527e8549214dbbbfc9722e5d867b5312dbc59a47aff
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzberg
import RustBridge

/// E2e tests for category: code.
final class CodeTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
        let _testDocs = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent() // <Module>Tests/
            .deletingLastPathComponent() // Tests/
            .deletingLastPathComponent() // swift_e2e/
            .deletingLastPathComponent() // e2e/
            .deletingLastPathComponent() // <repo root>
            .appendingPathComponent("test_documents")
        if FileManager.default.fileExists(atPath: _testDocs.path) {
            FileManager.default.changeCurrentDirectoryPath(_testDocs.path)
        }
    }

    func testCodeShebangDetection() throws {
        // Test language detection from shebang line via bytes input
        let result = try Kreuzberg.extractFileSync("code/script.sh", "text/x-source-code", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "text/x-source-code")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 10)
        XCTAssertTrue(result.content().toString().contains("build"), "expected to contain: \("build")")
        XCTAssertTrue(result.content().toString().contains("clean"), "expected to contain: \("clean")")
    }

}
