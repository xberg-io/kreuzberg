// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:31dc5a0d898b9f50b1b01b7771b76f553350023f15e9baab503e8c9c334ba8ca
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzberg
import RustBridge

/// E2e tests for category: async.
final class AsyncTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
        let _testDocs = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent() // <Module>Tests/
            .deletingLastPathComponent() // Tests/
            .deletingLastPathComponent() // swift_e2e/
            .deletingLastPathComponent() // e2e/
            .deletingLastPathComponent() // <repo root>
            .appendingPathComponent("test_documents")
        if FileManager.default.fileExists(atPath: _testDocs.path) {
            FileManager.default.changeCurrentDirectoryPath(_testDocs.path)
        }
    }

    func testAsyncExtractBytes() async throws {
        // Async extract_bytes call on PDF document
        let contentData = try Data(contentsOf: URL(fileURLWithPath: "pdf/fake_memo.pdf"))
        let contentBytes = Array(contentData)
        let result = try await Kreuzberg.extractBytes(contentBytes, "application/pdf", "{}")
        XCTAssertEqual(result.mimeType.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
    }

    func testAsyncExtractBytesEmptyMime() async throws {
        // extract_bytes empty MIME async
        do {
            let contentData = try Data(contentsOf: URL(fileURLWithPath: "text/plain.txt"))
            let contentBytes = Array(contentData)
            _ = try await Kreuzberg.extractBytes(contentBytes, "", "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAsyncExtractBytesInvalidMime() async throws {
        // extract_bytes unsupported MIME async
        do {
            let contentData = try Data(contentsOf: URL(fileURLWithPath: "text/plain.txt"))
            let contentBytes = Array(contentData)
            _ = try await Kreuzberg.extractBytes(contentBytes, "application/x-nonexistent", "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
