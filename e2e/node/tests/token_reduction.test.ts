// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a606af155ab354ea192c38e0c7b65ecc641668049a3fe3398950a0a6bb47f849
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, extractFileSync, reduceTokens, type JsExtractionConfig, type JsTokenReductionConfig } from 'kreuzberg';

describe('token_reduction', () => {
  it('token_reduction_aggressive: Tests aggressive token reduction mode significantly reduces content', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", { tokenReduction: { mode: "aggressive" } } as unknown as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(150);
    expect((() => { const v = (result.content as unknown); return typeof v === 'string' || Array.isArray(v) ? (v as { length: number }).length : Object.keys(v as object).length; })()).toBeGreaterThan(0);
  });

  it('token_reduction_basic: Tests basic token reduction on PDF document', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", { tokenReduction: { mode: "moderate" } } as unknown as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(200);
    expect((() => { const v = (result.content as unknown); return typeof v === 'string' || Array.isArray(v) ? (v as { length: number }).length : Object.keys(v as object).length; })()).toBeGreaterThan(0);
  });

  it('token_reduction_light: Tests light token reduction mode preserves most content', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", { tokenReduction: { mode: "light" } } as unknown as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect((() => { const v = (result.content as unknown); return typeof v === 'string' || Array.isArray(v) ? (v as { length: number }).length : Object.keys(v as object).length; })()).toBeGreaterThan(0);
  });

  it('token_reduction_reduce_aggressive: Reduce tokens with aggressive compression level', () => {
    const result = reduceTokens("The quick brown fox jumps over the lazy dog.", { level: "aggressive" } as unknown as JsTokenReductionConfig);
    expect(result.length).toBeGreaterThanOrEqual(1);
  });

  it('token_reduction_reduce_basic: Reduce tokens with light compression level', () => {
    const result = reduceTokens("The quick brown fox jumps over the lazy dog. This is a test sentence.", { level: "light" } as unknown as JsTokenReductionConfig);
    expect(result).toContain("quick");
  });

  it('token_reduction_with_chunking: Tests token reduction combined with chunking', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", { chunking: { maxCharacters: 500, overlap: 50 }, tokenReduction: { mode: "moderate" } } as unknown as JsExtractionConfig);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
    expect(result.content.length).toBeLessThanOrEqual(200);
    expect((() => { const v = (result.content as unknown); return typeof v === 'string' || Array.isArray(v) ? (v as { length: number }).length : Object.keys(v as object).length; })()).toBeGreaterThan(0);
    expect(result.chunks.length).toBeGreaterThanOrEqual(1);
    expect((result.chunks ?? []).every((c: { content?: string }) => !!c.content)).toBe(true);
  });
});
