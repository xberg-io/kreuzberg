// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3ce5cc9d874ead3e62ded58e389392b875ceb3c88f9ad37e3cf5e53d0c6c20c1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, embed_texts, type JsExtractionConfig } from 'kreuzberg';

describe('embed', () => {
  it('embed_async: Standalone embedding via async API path with balanced preset', () => {
    const result = embed_texts(["Async embedding test"] as JsExtractionConfig, { model: { name: "balanced", type: "preset" }, normalize: true } as JsExtractionConfig);
    expect(result.embeddings.length).toBe(1);
    expect(result.embeddingDimensions).toBe(768);
    expect(result.embeddingsValid).toBe(true);
    expect(result.embeddingsFinite).toBe(true);
    expect(result.embeddingsNonZero).toBe(true);
  });

  it('embed_batch_texts: Standalone embedding of a batch of 3 texts with fast preset', () => {
    const result = embed_texts(["Text one", "Text two", "Text three"] as JsExtractionConfig, { batch_size: 32, model: { name: "fast", type: "preset" }, normalize: true } as JsExtractionConfig);
    expect(result.embeddings.length).toBe(3);
    expect(result.embeddingDimensions).toBe(384);
    expect(result.embeddingsValid).toBe(true);
    expect(result.embeddingsFinite).toBe(true);
    expect(result.embeddingsNonZero).toBe(true);
    expect(result.embeddingsNormalized).toBe(true);
  });

  it('embed_empty_input: Standalone embedding with empty text list returns empty result without error', () => {
    const result = embed_texts([] as JsExtractionConfig, { model: { name: "balanced", type: "preset" } } as JsExtractionConfig);
    expect(result.embeddings.length).toBe(0);
    expect(result.embeddingsValid).toBe(true);
    expect(result.embeddingsFinite).toBe(true);
  });

  it('embed_multilingual: Standalone embedding with multilingual preset for non-English text', () => {
    const result = embed_texts(["Bonjour le monde", "Hallo Welt", "¡Hola Mundo!"] as JsExtractionConfig, { model: { name: "multilingual", type: "preset" }, normalize: true } as JsExtractionConfig);
    expect(result.embeddings.length).toBe(3);
    expect(result.embeddingDimensions).toBe(768);
    expect(result.embeddingsValid).toBe(true);
    expect(result.embeddingsFinite).toBe(true);
    expect(result.embeddingsNonZero).toBe(true);
  });

  it('embed_single_text: Standalone embedding of a single text with balanced preset', () => {
    const result = embed_texts(["Hello, Kreuzberg!"] as JsExtractionConfig, { model: { name: "balanced", type: "preset" }, normalize: true } as JsExtractionConfig);
    expect(result.embeddings.length).toBe(1);
    expect(result.embeddingDimensions).toBe(768);
    expect(result.embeddingsValid).toBe(true);
    expect(result.embeddingsFinite).toBe(true);
    expect(result.embeddingsNonZero).toBe(true);
    expect(result.embeddingsNormalized).toBe(true);
  });
});
