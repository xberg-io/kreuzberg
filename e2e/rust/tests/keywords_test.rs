// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:dc1dd77dced4df4877c2c98126e9edbe1b390c706508344bc22b17fc3ce85226
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: keywords

use kreuzberg::extract_file_sync;

#[test]
fn test_keywords_rake() {
    // Tests keyword extraction using RAKE algorithm
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"keywords": {"algorithm": "rake", "max_keywords": 10}});
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract_file_sync(path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(!result.keywords.is_empty(), "expected non-empty value");
    assert!(!result.keywords.is_empty(), "expected >= 1");
    assert!(result.keywords_count <= 10, "expected <= 10");
}

#[test]
fn test_keywords_yake() {
    // Tests keyword extraction using YAKE algorithm
    let path = r#"pdf/fake_memo.pdf"#;
    let mime_type: Option<String> = None;
    let config_json = serde_json::json!({"keywords": {"algorithm": "yake", "max_keywords": 10}});
    let config = serde_json::from_value(config_json).unwrap();
    let result = extract_file_sync(path, mime_type.as_deref(), &config).expect("should succeed");
    assert_eq!(result.mime_type.trim(), r#"application/pdf"#, "equals assertion failed");
    assert!(
        result.content.len() >= 10,
        "expected length >= 10, got {}",
        result.content.len()
    );
    assert!(!result.keywords.is_empty(), "expected non-empty value");
    assert!(!result.keywords.is_empty(), "expected >= 1");
    assert!(result.keywords_count <= 10, "expected <= 10");
}
