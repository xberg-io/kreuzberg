// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d356a10ec8ed8b3bb05a833ad4266ec19ee3cc196c88bad5a9271b556f4e388d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: embed

use kreuzberg::embed_texts;

#[test]
fn test_embed_async() {
    // Standalone embedding via async API path with balanced preset
    let texts_json = serde_json::json!(["Async embedding test"]);
    let texts = serde_json::from_value::<Vec<String>>(texts_json).unwrap();
    let config_json = serde_json::json!({"model": {"name": "balanced", "type": "preset"}, "normalize": true});
    let config = serde_json::from_value(config_json).unwrap();
    let result = embed_texts(&texts, &config).expect("should succeed");
    assert_eq!(
        result.embeddings.len(),
        1,
        "expected exactly 1 elements, got {}",
        result.embeddings.len()
    );
    assert_eq!(result.embedding_dimensions, 768, "equals assertion failed");
    assert!(result.embeddings_valid, "expected true");
    assert!(result.embeddings_finite, "expected true");
    assert!(result.embeddings_non_zero, "expected true");
}

#[test]
fn test_embed_batch_texts() {
    // Standalone embedding of a batch of 3 texts with fast preset
    let texts_json = serde_json::json!(["Text one", "Text two", "Text three"]);
    let texts = serde_json::from_value::<Vec<String>>(texts_json).unwrap();
    let config_json =
        serde_json::json!({"batch_size": 32, "model": {"name": "fast", "type": "preset"}, "normalize": true});
    let config = serde_json::from_value(config_json).unwrap();
    let result = embed_texts(&texts, &config).expect("should succeed");
    assert_eq!(
        result.embeddings.len(),
        3,
        "expected exactly 3 elements, got {}",
        result.embeddings.len()
    );
    assert_eq!(result.embedding_dimensions, 384, "equals assertion failed");
    assert!(result.embeddings_valid, "expected true");
    assert!(result.embeddings_finite, "expected true");
    assert!(result.embeddings_non_zero, "expected true");
    assert!(result.embeddings_normalized, "expected true");
}

#[test]
fn test_embed_empty_input() {
    // Standalone embedding with empty text list returns empty result without error
    let texts_json = serde_json::json!([]);
    let texts = serde_json::from_value::<Vec<String>>(texts_json).unwrap();
    let config_json = serde_json::json!({"model": {"name": "balanced", "type": "preset"}});
    let config = serde_json::from_value(config_json).unwrap();
    let result = embed_texts(&texts, &config).expect("should succeed");
    assert_eq!(
        result.embeddings.len(),
        0,
        "expected exactly 0 elements, got {}",
        result.embeddings.len()
    );
    assert!(result.embeddings_valid, "expected true");
    assert!(result.embeddings_finite, "expected true");
}

#[test]
fn test_embed_multilingual() {
    // Standalone embedding with multilingual preset for non-English text
    let texts_json = serde_json::json!(["Bonjour le monde", "Hallo Welt", "¡Hola Mundo!"]);
    let texts = serde_json::from_value::<Vec<String>>(texts_json).unwrap();
    let config_json = serde_json::json!({"model": {"name": "multilingual", "type": "preset"}, "normalize": true});
    let config = serde_json::from_value(config_json).unwrap();
    let result = embed_texts(&texts, &config).expect("should succeed");
    assert_eq!(
        result.embeddings.len(),
        3,
        "expected exactly 3 elements, got {}",
        result.embeddings.len()
    );
    assert_eq!(result.embedding_dimensions, 768, "equals assertion failed");
    assert!(result.embeddings_valid, "expected true");
    assert!(result.embeddings_finite, "expected true");
    assert!(result.embeddings_non_zero, "expected true");
}

#[test]
fn test_embed_single_text() {
    // Standalone embedding of a single text with balanced preset
    let texts_json = serde_json::json!(["Hello, Kreuzberg!"]);
    let texts = serde_json::from_value::<Vec<String>>(texts_json).unwrap();
    let config_json = serde_json::json!({"model": {"name": "balanced", "type": "preset"}, "normalize": true});
    let config = serde_json::from_value(config_json).unwrap();
    let result = embed_texts(&texts, &config).expect("should succeed");
    assert_eq!(
        result.embeddings.len(),
        1,
        "expected exactly 1 elements, got {}",
        result.embeddings.len()
    );
    assert_eq!(result.embedding_dimensions, 768, "equals assertion failed");
    assert!(result.embeddings_valid, "expected true");
    assert!(result.embeddings_finite, "expected true");
    assert!(result.embeddings_non_zero, "expected true");
    assert!(result.embeddings_normalized, "expected true");
}
