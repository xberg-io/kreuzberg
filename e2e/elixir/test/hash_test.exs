# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d198d4032ff3ac0b71ad30266fe09b7aba8a7a6d8c950808017e0eea36140090
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: hash
defmodule E2e.HashTest do
  use ExUnit.Case, async: true

  describe "blake3_bytes_pdf" do
    test "Hash PDF bytes using BLAKE3" do
      {:ok, result} = Kreuzberg.blake3_hash_bytes("")
      assert String.length(result) >= 64
    end
  end

  describe "blake3_bytes_text" do
    test "Hash text file bytes using BLAKE3" do
      {:ok, result} = Kreuzberg.blake3_hash_bytes("")
      assert String.length(result) >= 64
    end
  end

  describe "blake3_file_not_found" do
    test "blake3_hash_file nonexistent file" do
      assert {:error, _} = Kreuzberg.blake3_hash_file("nonexistent/file.pdf")
    end
  end

  describe "blake3_file_pdf" do
    test "Hash PDF file using BLAKE3" do
      {:ok, result} = Kreuzberg.blake3_hash_file("pdf/fake_memo.pdf")
      assert String.length(result) >= 64
    end
  end

  describe "blake3_file_text" do
    test "Hash text file using BLAKE3" do
      {:ok, result} = Kreuzberg.blake3_hash_file("text/plain.txt")
      assert String.length(result) >= 64
    end
  end

  describe "compute_hash_basic" do
    test "Compute hash for PDF file" do
      {:ok, result} = Kreuzberg.compute_hash("")
    end
  end

  describe "compute_hash_different" do
    test "Hash different content" do
      {:ok, result} = Kreuzberg.compute_hash("")
    end
  end

  describe "fast_hash_basic" do
    test "Hash PDF file using fast hash" do
      {:ok, result} = Kreuzberg.fast_hash("")
    end
  end

  describe "fast_hash_empty" do
    test "Hash empty bytes" do
      {:ok, result} = Kreuzberg.fast_hash("")
    end
  end
end
