# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:e0584e74f7d63d9e9246262db584b4029d36e7b9782018e065f9449534f1c7d4
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: html
defmodule E2e.HtmlTest do
  use ExUnit.Case, async: true

  describe "html_complex_layout" do
    test "Large Wikipedia HTML page to validate complex conversion." do
      {:ok, result} = Kreuzberg.extract_file_async("html/taylor_swift.html")
      assert String.trim(result.mime_type) == "text/html"
      assert String.length(result.content) >= 1000
    end
  end

  describe "html_simple_table" do
    test "HTML table converted to markdown should retain structure." do
      {:ok, result} = Kreuzberg.extract_file_async("html/simple_table.html")
      assert String.trim(result.mime_type) == "text/html"
      assert String.length(result.content) >= 100
      assert String.contains?(to_string(result.content), "Product")
      assert String.contains?(to_string(result.content), "Category")
      assert String.contains?(to_string(result.content), "Price")
      assert String.contains?(to_string(result.content), "Stock")
      assert String.contains?(to_string(result.content), "Laptop")
      assert String.contains?(to_string(result.content), "Electronics")
      assert String.contains?(to_string(result.content), "Sample Data Table")
    end
  end
end
