# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:80e57ce6a99fc03d7aa32158f6f9ede668c08d7a293a271ed044021a25916ee2
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: async
defmodule E2e.AsyncTest do
  use ExUnit.Case, async: true

  describe "async_extract_bytes" do
    test "Async extract_bytes call on PDF document" do
      {:ok, result} = Kreuzberg.extract_bytes_async("pdf/fake_memo.pdf", "application/pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 50
    end
  end

  describe "async_extract_bytes_empty_mime" do
    test "extract_bytes empty MIME async" do
      assert {:error, _} = Kreuzberg.extract_bytes_async(nil, "", %{})
    end
  end

  describe "async_extract_bytes_invalid_mime" do
    test "extract_bytes unsupported MIME async" do
      assert {:error, _} = Kreuzberg.extract_bytes_async(nil, "application/x-nonexistent", %{})
    end
  end

  describe "async_extract_file" do
    test "Async extract_file call on PDF document" do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/fake_memo.pdf", "application/pdf")
      assert String.trim(result.mime_type) == "application/pdf"
      assert String.length(result.content) >= 50
    end
  end

  describe "async_extract_file_docx" do
    test "Async extract_file call on DOCX document" do
      {:ok, result} = Kreuzberg.extract_file_async("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
      assert String.length(result.content) >= 20
    end
  end

  describe "async_extract_file_html" do
    test "Async extract_file call on HTML document" do
      {:ok, result} = Kreuzberg.extract_file_async("html/simple_table.html", "text/html")
      assert String.length(result.content) >= 10
    end
  end

  describe "async_extract_file_image" do
    test "Async extract_file call on image document" do
      {:ok, result} = Kreuzberg.extract_file_async("images/test_hello_world.png", "image/png")
    end
  end

  describe "async_extract_file_with_config" do
    test "Async extract_file call with output format and chunking config" do
      {:ok, result} = Kreuzberg.extract_file_async("pdf/fake_memo.pdf", "application/pdf", %{"chunking" => %{"max_characters" => 500, "max_overlap" => 50}, "output_format" => "markdown"})
      assert String.length(result.content) >= 10
    end
  end
end
