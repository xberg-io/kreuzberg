// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cce3f647fce1301fa2c5d30b0ac7e7ffbbaa3e2221cf814cf7df129f4fa0e362
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, it, expect } from 'vitest';
import { extract_file, extract_file_sync, WasmExtractionConfig } from 'kreuzberg';

describe('keywords', () => {
  it('keywords_rake: Tests keyword extraction using RAKE algorithm', () => {
    const options = new WasmExtractionConfig();
    options.keywords = { algorithm: "rake", max_keywords: 10 };
    const result = extract_file_sync("pdf/fake_memo.pdf", options);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.keywords.length).toBeGreaterThan(0);
    expect(result.keywords.length).toBeGreaterThanOrEqual(1);
    expect(result.keywordsCount).toBeLessThanOrEqual(10);
  });

  it('keywords_yake: Tests keyword extraction using YAKE algorithm', () => {
    const options = new WasmExtractionConfig();
    options.keywords = { algorithm: "yake", max_keywords: 10 };
    const result = extract_file_sync("pdf/fake_memo.pdf", options);
    expect(result.mimeType.trim()).toBe("application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.keywords.length).toBeGreaterThan(0);
    expect(result.keywords.length).toBeGreaterThanOrEqual(1);
    expect(result.keywordsCount).toBeLessThanOrEqual(10);
  });
});
