// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4a703a1909f82b4e539a41133a633f40be574d771fa737d7b4faca231437087d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, it, expect } from 'vitest';
import { extract_file, extract_file_sync, WasmExtractionConfig } from 'kreuzberg';

describe('document', () => {
  it('doc_metadata_docx: Extract DOCX metadata', () => {
    const result = extract_file_sync("vendored/markitdown/test.docx");
    expect(result.metadata.length).toBeGreaterThan(0);
  });

  it('doc_metadata_pdf: Extract PDF metadata', () => {
    const result = extract_file_sync("pdf/fake_memo.pdf");
    expect(result.metadata.length).toBeGreaterThan(0);
  });

  it('doc_structure_docx: Extract DOCX document structure with include_document_structure config', () => {
    const options = new WasmExtractionConfig();
    options.includeDocumentStructure = true;
    const result = extract_file_sync("vendored/markitdown/test.docx", options);
    expect(result.documentStructure.length).toBeGreaterThan(0);
  });

  it('doc_structure_html: Extract HTML document structure with include_document_structure config', () => {
    const options = new WasmExtractionConfig();
    options.includeDocumentStructure = true;
    const result = extract_file_sync("html/html.html", options);
    expect(result.documentStructure.length).toBeGreaterThan(0);
  });

  it('doc_structure_pdf: Extract PDF document structure with include_document_structure config', () => {
    const options = new WasmExtractionConfig();
    options.includeDocumentStructure = true;
    const result = extract_file_sync("pdf/fake_memo.pdf", options);
    expect(result.documentStructure.length).toBeGreaterThan(0);
  });
});
