// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1f240891f2a950d860b5a837968f28c8589c364abd771a543a8e6a7efa5ec7b7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, bold, code, italic, link, strikethrough, underline } from 'kreuzberg';

describe('markup', () => {
  it('bold_basic: Create bold markdown formatting', () => {
    const result = bold(0, 9);
    expect(result.start).toBe(0);
    expect(result.end).toBe(9);
  });

  it('bold_empty: Bold empty string', () => {
    bold(0, 0);
  });

  it('bold_special_chars: Bold special chars', () => {
    const result = bold(0, 23);
    expect(result.start).toBe(0);
    expect(result.end).toBe(23);
  });

  it('code_basic: Create inline code formatting', () => {
    const result = code(0, 8);
    expect(result.start).toBe(0);
    expect(result.end).toBe(8);
  });

  it('code_multiline: Code multiline', () => {
    const result = code(0, 11);
    expect(result.start).toBe(0);
    expect(result.end).toBe(11);
  });

  it('italic_basic: Create italic markdown formatting', () => {
    const result = italic(0, 8);
    expect(result.start).toBe(0);
    expect(result.end).toBe(8);
  });

  it('italic_empty: Italic empty string', () => {
    italic(0, 0);
  });

  it('italic_special_chars: Italic special chars', () => {
    const result = italic(0, 17);
    expect(result.start).toBe(0);
    expect(result.end).toBe(17);
  });

  it('link_basic: Create markdown link with text and URL', () => {
    const result = link(0, 10, "https://example.com");
    expect(result.start).toBe(0);
    expect(result.end).toBe(10);
  });

  it('link_empty_text: Create markdown link with empty text', () => {
    const result = link(0, 0, "https://example.com");
    expect(result.start).toBe(0);
    expect(result.end).toBe(0);
  });

  it('link_special_url: Link with query params', () => {
    const result = link(0, 5, "https://example.com?q=1&b=2");
    expect(result.start).toBe(0);
    expect(result.end).toBe(5);
  });

  it('strikethrough_basic: Create strikethrough formatting', () => {
    const result = strikethrough(0, 7);
    expect(result.start).toBe(0);
    expect(result.end).toBe(7);
  });

  it('strikethrough_empty: Strikethrough empty', () => {
    strikethrough(0, 0);
  });

  it('underline_basic: Create underline formatting', () => {
    const result = underline(0, 10);
    expect(result.start).toBe(0);
    expect(result.end).toBe(10);
  });

  it('underline_empty: Underline empty', () => {
    underline(0, 0);
  });
});
