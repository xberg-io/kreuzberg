// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:263e784a4bedd6b0d83b1fb1e2751b87bb91022b9921ddbd381e7b02a816bc7c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, it, expect } from 'vitest';
import { extract_file, bold, code, italic, link, strikethrough, underline } from 'kreuzberg';

describe('markup', () => {
  it('bold_basic: Create bold markdown formatting', () => {
    const result = bold(null, null);
    expect(result.result).toContain("important");
  });

  it('bold_empty: Bold empty string', () => {
    bold(null, null);
  });

  it('bold_special_chars: Bold special chars', () => {
    const result = bold(null, null);
    expect(result.result).toContain("hello");
  });

  it('code_basic: Create inline code formatting', () => {
    const result = code(null, null);
    expect(result.result).toContain("variable");
  });

  it('code_multiline: Code multiline', () => {
    code(null, null);
  });

  it('italic_basic: Create italic markdown formatting', () => {
    const result = italic(null, null);
    expect(result.result).toContain("emphasis");
  });

  it('italic_empty: Italic empty string', () => {
    italic(null, null);
  });

  it('italic_special_chars: Italic special chars', () => {
    italic(null, null);
  });

  it('link_basic: Create markdown link with text and URL', () => {
    const result = link(null, null, "https://example.com");
    expect(result.result).toContain("Click here");
    expect(result.result).toContain("example.com");
  });

  it('link_empty_text: Create markdown link with empty text', () => {
    const result = link(null, null, "https://example.com");
    expect(result.result).toContain("example.com");
  });

  it('link_special_url: Link with query params', () => {
    link(null, null, "https://example.com?q=1&b=2");
  });

  it('strikethrough_basic: Create strikethrough formatting', () => {
    const result = strikethrough(null, null);
    expect(result.result).toContain("deleted");
  });

  it('strikethrough_empty: Strikethrough empty', () => {
    strikethrough(null, null);
  });

  it('underline_basic: Create underline formatting', () => {
    const result = underline(null, null);
    expect(result.result).toContain("underlined");
  });

  it('underline_empty: Underline empty', () => {
    underline(null, null);
  });
});
